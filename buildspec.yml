version: 0.2

phases:
  install:
    commands:
      - echo Installing Maven
      - yum install -y maven

  build:
    commands:
      - echo Build started
      - mvn clean install

  post_build:
    commands:
      - echo Build completed
